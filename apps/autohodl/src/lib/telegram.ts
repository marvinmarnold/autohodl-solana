// Re-exports from @autohodl/blinks-telegram — single source of truth for
// initData validation and the TelegramUser type across this app.
export { validateInitData, InvalidInitDataError, type TelegramUser } from "@autohodl/blinks-telegram/server";
